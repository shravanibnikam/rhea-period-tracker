// domain/constants.ts — cycle-math constants (moved from lib/constants.ts,
// M1.2). UI option lists (symptom labels, flow colors, mood options) are
// presentation content and stay in lib/constants.ts.

export const DEFAULT_CYCLE_LENGTH = 28;
export const DEFAULT_LUTEAL_LENGTH = 14;
export const MIN_CYCLES_FOR_PREDICTION = 1;
export const ROLLING_AVERAGE_WINDOW = 6;
